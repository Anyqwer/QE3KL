#pragma once

namespace f
{
    namespace autopistol
    {
        // Initialize mouse hook for auto-pistol
        void initialize();
        
        // Cleanup mouse hook
        void cleanup();
    }
}
